apiVersion: v1
kind: Service
metadata:
  namespace: finstack
    namespace: finstack
  name: finstack-transaction-service
spec:
  type: ClusterIP
  selector:
    app: finstack-transation
  ports:
  - port: 4004
    targetPort: 4004
