apiVersion: v1
kind: Service
metadata:
  name: finstack-mongodb-service
  namespace: finstack
spec:
  selector:
    app: finstack-mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017
  type: ClusterIP
