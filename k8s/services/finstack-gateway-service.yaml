apiVersion: v1
kind: Service
metadata:
  namespace: finstack
    namespace: finstack
  name: finstack-gateway-service
spec:
  type: ClusterIP
  selector:
    app: finstack-gateway
  ports:
  - port: 3000
    targetPort: 3000
