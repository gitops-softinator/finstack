apiVersion: v1
kind: Service
metadata:
  name: finstack-payment-service
  namespace: finstack
spec:
  selector:
    app: finstack-payment
  ports:
    - protocol: TCP
      port: 4002
      targetPort: 4002
  type: ClusterIP
