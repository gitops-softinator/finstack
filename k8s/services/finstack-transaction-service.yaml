apiVersion: v1
kind: Service
metadata:
  name: finstack-transaction-service
  namespace: finstack
spec:
  selector:
    app: finstack-transaction
  ports:
    - protocol: TCP
      port: 4004
      targetPort: 4004
  type: ClusterIP
