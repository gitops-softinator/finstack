apiVersion: v1
kind: Service
metadata:
  name: finstack-notification-service
  namespace: finstack
spec:
  selector:
    app: finstack-notification
  ports:
    - protocol: TCP
      port: 4003
      targetPort: 4003
  type: ClusterIP
