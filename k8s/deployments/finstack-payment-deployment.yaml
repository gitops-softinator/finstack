apiVersion: apps/v1
kind: Deployment
metadata:
  name: finstack-payment
  namespace: finstack
spec:
  replicas: 1
  selector:
    matchLabels:
      app: finstack-payment
  template:
    metadata:
      labels:
        app: finstack-payment
    spec:
      containers:
      - name: finstack-payment
        image: gitopssoftinator/finstack-payment-service:1.1
        ports:
        - containerPort: 4002
