apiVersion: v1
kind: Service
metadata:
  namespace: finstack
    namespace: finstack
  name: finstack-user-service
spec:
  type: ClusterIP
  selector:
    app: finstack-user
  ports:
   - port: 4001
     targetPort: 4001
