apiVersion: v1
kind: Service
metadata:
  namespace: finstack
    namespace: finstack
  name: finstack-auth-service
spec:
  type: ClusterIP
  selector:
    app: finstack-auth
  ports:
  - port: 4000
    targetPort: 4000
